/**
 * Register all migrations in order.
 * Import this file once at startup (from database.ts) to populate the registry.
 */
import './001-add-intrinsic-column.js';
import './002-add-session-tables.js';
